// Optional bonus feature stubs. Do not register this router in app.js unless you want bonus features enabled.
//
// import express from 'express';
// import { authenticate, authorize } from '../middleware/auth.js';
// const router = express.Router();
// router.use(authenticate);
//
// router.get('/testcases/export', authorize('admin', 'test-lead'), async (_req, res) => {
//   res.status(501).json({ message: 'CSV/Excel export placeholder' });
// });
//
// router.post('/testcases/import', authorize('admin', 'test-lead'), async (_req, res) => {
//   res.status(501).json({ message: 'CSV/Excel import placeholder' });
// });
//
// router.post('/notifications/test-assignment', authorize('admin', 'test-lead'), async (_req, res) => {
//   res.status(501).json({ message: 'Email notification placeholder' });
// });
//
// router.post('/integrations/ci/test-results', authorize('admin'), async (_req, res) => {
//   res.status(501).json({ message: 'CI/CD integration placeholder' });
// });
//
// router.post('/test-runs/schedule', authorize('admin', 'test-lead'), async (_req, res) => {
//   res.status(501).json({ message: 'Test run scheduling placeholder' });
// });
//
// router.post('/testcases/:id/comments', authorize('admin', 'test-lead', 'tester'), async (_req, res) => {
//   res.status(501).json({ message: 'Collaborative comments placeholder' });
// });
//
// export default router;
